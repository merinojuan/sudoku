import { DifficultyNamePipe } from './difficulty-name.pipe';

describe('DifficultyNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DifficultyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
